#!/bin/perl

$layers_dir="/root/layers";

#which dir are we in
$pwd = `pwd`;
chomp($pwd);

my @all_overlayfs = `mount | grep overlay | cut -d' ' -f 1`;

system("cd /root");

foreach(@all_overlayfs)
{
	#system("umount $_");
	print $_;
}

#$delete_all_layers_cmd ="rm -rf $layers_dir/*";

#print $delete_all_layers_cmd;

#system($delete_all_layers_cmd);

print "\n";
