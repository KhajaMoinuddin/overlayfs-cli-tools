#!/bin/perl

use lib '/root/overlayfs_tools';
use overlayfs;

$base_dir = $ARGV[0];
$layer_tag = $ARGV[1];
$layer_tag =~ s/[\s]/_/g;

$layers_dir="/root/layers";

bring_layer_to_front($base_dir,$layer_tag);
